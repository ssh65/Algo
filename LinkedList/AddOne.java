package LinkedList;

public class AddOne {
    public static void main(String[] args) {
        LinkedList list = new LinkedList();
        LinkedList.Insert(list, 1);
        LinkedList.Insert(list, 9);
        LinkedList.Insert(list, 9);
        LinkedList.Insert(list, 9);
        LinkedList.Display(list);
        Addone(list);
        LinkedList.Display(list);
    }

    static void Addone(LinkedList list){
        
    }
}
